% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sfpca.R
\name{sfpca}
\alias{sfpca}
\title{Sparse and Functional Principal Components Analysis (SFPCA) with Spatial Coordinates}
\usage{
sfpca(
  X,
  K,
  spat_cds,
  lambda_u = NULL,
  lambda_v = NULL,
  alpha_u = NULL,
  alpha_v = NULL,
  Omega_u = NULL,
  penalty_u = "l1",
  penalty_v = "l1",
  nlambda = 10,
  lambda_min_ratio = 0.01,
  knn = min(6, ncol(X) - 1),
  max_iter = 100,
  tol = 1e-06,
  verbose = FALSE,
  uthresh = NULL,
  vthresh = NULL
)
}
\arguments{
\item{X}{A numeric data matrix of dimensions n (observations/time points) by p (variables/space).}

\item{K}{The number of principal components to estimate.}

\item{spat_cds}{A matrix of spatial coordinates for each column of X (variables). Each row
corresponds to a spatial dimension (e.g., x, y, z), and each column corresponds to a variable.
Note the orientation: this is \verb{dimensions x variables}, so
\code{ncol(spat_cds)} must equal \code{ncol(X)} -- the transpose of the layout a
coordinate data frame usually has. For a one-dimensional axis (a spectrum,
a transect) pass \code{matrix(coords, nrow = 1)}.}

\item{lambda_u}{Sparsity penalty parameter for u. If NULL, selected per component by BIC
along a regularization path (see Details).}

\item{lambda_v}{Sparsity penalty parameter for v. If NULL, selected per component by BIC
along a regularization path (see Details).}

\item{alpha_u}{Smoothness penalty parameter for u. If NULL, defaults to
\code{1 / lambda_max(Omega_u)} (see Details).}

\item{alpha_v}{Smoothness penalty parameter for v. If NULL, defaults to
\code{1 / lambda_max(Omega_v)} (see Details).}

\item{Omega_u}{A positive semi-definite matrix for smoothness penalty on u. If NULL, defaults to
second differences penalty (sparse matrix). Unlike \code{Omega_u}, there is no
corresponding \code{Omega_v} argument: the column-side smoothness penalty is
always built internally from \code{spat_cds} (via \code{knn}); supplying a custom
\code{Omega_v} is not currently supported.}

\item{penalty_u}{The penalty function for u. Either "l1" (lasso, the
default) or "scad".}

\item{penalty_v}{The penalty function for v. Either "l1" (lasso, the
default) or "scad".}

\item{nlambda}{Number of values on the regularization path used for BIC selection
of \code{lambda_u}/\code{lambda_v} when they are NULL. Default \code{10}.}

\item{lambda_min_ratio}{Smallest path value as a fraction of the closed-form
\code{lambda_max}, on a log-spaced grid. Default \code{1e-2}.}

\item{knn}{Number of nearest neighbours for constructing \code{Omega_v}. Default
\code{min(6, ncol(X) - 1)}.}

\item{max_iter}{Maximum number of iterations for the alternating optimization.
Default \code{100}.}

\item{tol}{Tolerance for convergence of the rank-1 objective. Default \code{1e-6}.}

\item{verbose}{Logical; if TRUE, prints progress messages.}

\item{uthresh}{Deprecated and ignored; \code{lambda_u} is now selected by BIC.}

\item{vthresh}{Deprecated and ignored; \code{lambda_v} is now selected by BIC.}
}
\value{
An object of class \code{c("sfpca", "bi_projector")} from the
\pkg{multivarious} framework. Use \code{multivarious::scores()} for the sample
scores (\eqn{U D}), \code{multivarious::components()} for the sparse loadings
\eqn{V}, \code{multivarious::sdev()} for \eqn{d_k}, and
\code{multivarious::reconstruct()} for the rank-\code{K} approximation. \code{ov} (like
\code{components()}) holds the sparse right factors \eqn{V}; \code{ou} holds the
left factors \eqn{U}. The selected penalty parameters are stored as
\code{lambda_u}, \code{lambda_v}, \code{alpha_u}, and \code{alpha_v}. For backward
compatibility the pre-0.1 list fields \verb{$d} (singular values) and \verb{$u}
(left factors) remain readable but emit a deprecation warning; use
\code{sdev()} and \code{scores()}/\verb{$ou} instead.

\strong{Important:} unlike \code{genpca()}, the columns of \code{U} (\code{ou}) and \code{V}
(\code{ov}) are Euclidean unit-norm but are \strong{not} mutually orthogonal
across components -- \code{sfpca()} extracts each rank-1 term from a
constraint-form subproblem rather than a joint SVD, so \verb{U'U != I} and
\verb{V'V != I} in general. Consequently \code{multivarious::sdev()} here is
\emph{not} the singular values of \code{X}; it is the per-component captured
covariance \eqn{d_k = u_k' X_k v_k}, where \eqn{X_k} is the matrix after
the preceding components have been deflated out (so the identity holds
against \code{X} itself only for \eqn{k = 1}). This non-orthogonality is also why
\code{reconstruct()} for \code{"sfpca"} objects uses the stored \code{U}, \code{d}, \code{V}
factors directly (\verb{U D V'}) rather than SVD-based identities such as the
Moore-Penrose pseudoinverse of the loadings, which would not reproduce
the fitted model for non-orthogonal \code{V} (see \code{reconstruct.sfpca()}).
}
\description{
Performs Sparse and Functional PCA on a data matrix, allowing for both sparsity and smoothness
in the estimated principal components. Penalty parameters left \code{NULL} are selected
automatically (see Details). The spatial smoothness penalty is constructed based on
provided spatial coordinates.
}
\details{
Each rank-1 problem is solved by alternating solves of the penalized
quadratic subproblems (via C++ coordinate descent) followed by rescaling
onto the smoothness-metric ball, in the constraint form of Allen & Weylandt
(2019). For the convex \code{"l1"} penalty with subproblems solved to tolerance
(the internal \code{exact_inner = TRUE} path, used by the monotonicity test) the
objective is monotonically non-decreasing; the default inexact path
tightens the inner tolerance to a floor before it may declare convergence,
reproducing the same terminal iterates but without an every-iteration
monotonicity guarantee (it may also stop at \code{max_iter}).

When \code{lambda_u} or \code{lambda_v} is \code{NULL} it is selected per component by a
BIC-style criterion along a regularization path. For the convex \code{"l1"}
penalty \code{lambda_max = max(abs(b))} is, in closed form, the smallest value
whose subproblem solution is exactly zero (at \code{x = 0} the \verb{S x} term
vanishes, so the KKT condition \verb{|b_j| <= lambda} does not depend on \code{S});
where \code{b} is the matrix-vector product with the other factor fixed at the
SVD initializer. For the non-convex \code{"scad"} penalty the same value anchors
the path but is not a global-optimality threshold. \code{nlambda} values are
laid log-spaced down to \code{lambda_min_ratio * lambda_max}, coordinate descent
is warm-started along the path, and the value minimizing
\verb{log(RSS / (n p)) + df * log(n p) / (n p)} is chosen, with \code{df} the support
size of the solution and \code{RSS} the one-sided rank-1 residual sum of squares
with the opposite factor held fixed (a selection heuristic, not the BIC of
the fully alternated rank-1 model). The all-zero solution (at \code{lambda_max})
is a legitimate candidate: if no rank-1 structure justifies its degrees of
freedom, the component is returned as exactly zero with \code{d = 0}.

When \code{alpha_u} or \code{alpha_v} is \code{NULL} it defaults to
\code{1 / lambda_max(Omega)}, so the roughest direction of the smoothness
penalty is weighted exactly as strongly as the identity term. This makes
the default invariant to the scaling of \code{Omega} and bounds the condition
number of every subproblem system \code{I + alpha * Omega} by 2.
}
\examples{
library(Matrix)
set.seed(123)
# Smooth temporal factor, sparse spatial factor
n <- 100  # Number of time points
p <- 50   # Number of spatial locations
u <- sin(seq(0, 2 * pi, length.out = n))
v <- c(rnorm(10), rep(0, p - 10))
X <- 8 * tcrossprod(u / sqrt(sum(u^2)), v / sqrt(sum(v^2))) +
  matrix(rnorm(n * p, sd = 0.2), n, p)
spat_cds <- matrix(runif(p * 3), nrow = 3, ncol = p)  # 3D coordinates
result <- sfpca(X, K = 1, spat_cds = spat_cds)
multivarious::sdev(result)                  # captured covariance (BIC-tuned)
sum(multivarious::components(result) != 0)  # sparse spatial loading
}
\references{
Allen, G. I., & Weylandt, M. (2019). Sparse and functional
principal components analysis. In \emph{2019 IEEE Data Science Workshop
(DSW)} (pp. 11-16). \doi{10.1109/DSW.2019.8755778}. Also available as
\href{https://arxiv.org/abs/1309.2895}{arXiv:1309.2895}, first posted in
2013 and revised through 2019; the preprint and the DSW paper are the
same work, which is why both years appear in the literature.
}
\seealso{
\code{\link[=genpca]{genpca()}} for the shared \pkg{multivarious} verbs;
\code{multivarious::bi_projector}.
}
