% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpca.R
\name{truncate.genpca}
\alias{truncate.genpca}
\title{Truncate a genpca fit to fewer components}
\usage{
\method{truncate}{genpca}(x, ncomp)
}
\arguments{
\item{x}{A \code{genpca} object.}

\item{ncomp}{Number of components to retain (a positive integer no larger
than \code{ncomp(x)}).}
}
\value{
A \code{genpca} object with \code{ncomp} components.
}
\description{
Returns a new \code{genpca} object retaining only the first \code{ncomp} components.
All component-indexed slots (\code{v}, \code{s}, \code{sdev}, \code{ov}, \code{ou}, \code{u}, \code{propv},
\code{cumv}) are sliced consistently; the preprocessing object and constraint
matrices are carried over unchanged.
}
\examples{
X <- matrix(rnorm(60), 15, 4)
fit <- genpca(X, ncomp = 4)
fit2 <- truncate(fit, 2)
multivarious::ncomp(fit2)
}
\seealso{
\code{\link[=genpca]{genpca()}}, \code{\link[=reconstruct.genpca]{reconstruct.genpca()}}
}
