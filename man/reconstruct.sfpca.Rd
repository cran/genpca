% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sfpca.R
\name{reconstruct.sfpca}
\alias{reconstruct.sfpca}
\title{Reconstruct data from an sfpca fit}
\usage{
\method{reconstruct}{sfpca}(
  x,
  comp = seq_len(multivarious::ncomp(x)),
  rowind = NULL,
  colind = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{sfpca} object.}

\item{comp}{Integer vector of components to use (default: all).}

\item{rowind}{Optional integer vector of rows to reconstruct (default: all).}

\item{colind}{Optional integer vector of columns to reconstruct (default:
all).}

\item{...}{Ignored.}
}
\value{
A numeric matrix of dimension \verb{length(rowind) x length(colind)},
the rank-\code{length(comp)} reconstruction \eqn{U D V'} using sfpca's
stored non-orthogonal factors.
}
\description{
Reconstructs the rank-\code{K} \code{\link{sfpca}} model as \eqn{U D V'},
using the stored (non-orthogonal) factors directly.
}
\details{
sfpca components are Euclidean unit vectors but are \strong{not} mutually
orthogonal, so \eqn{V'V \ne I}. The inherited \code{reconstruct.bi_projector()}
method reconstructs through the Moore-Penrose pseudoinverse of the
loadings (\verb{scores \\\%*\\\% pinv(V)}), which for non-orthogonal \code{V} does
\strong{not} return the rank-\code{comp} model \eqn{U D V'} that \code{sfpca()} actually
fits and deflates with. This method instead computes
\verb{scores(x)[rowind, comp] \\\%*\\\% t(components(x)[colind, comp])}, i.e.
\eqn{U D V'} restricted to the requested rows/columns/components.
\code{sfpca()} does no preprocessing, so no inverse transform is applied.
}
\seealso{
\code{\link[=sfpca]{sfpca()}}
}
