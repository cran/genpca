% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gplssvd_op.R
\name{gplssvd_op}
\alias{gplssvd_op}
\title{Generalized PLS-SVD via Implicit Operator (memory-safe)}
\usage{
gplssvd_op(
  X,
  Y,
  XLW = NULL,
  YLW = NULL,
  XRW = NULL,
  YRW = NULL,
  k = 2,
  center = FALSE,
  scale = FALSE,
  svd_backend = c("eigencore", "irlba", "RSpectra"),
  svd_opts = list(tol = 1e-07, maxitr = 1000),
  constraints_remedy = c("error", "ridge", "clip", "identity")
)
}
\arguments{
\item{X}{n x I matrix (numeric or Matrix)}

\item{Y}{n x J matrix (numeric or Matrix)}

\item{XLW}{Row metric for X (M_X): NULL/identity, numeric length-n, diagonalMatrix, or PSD Matrix}

\item{YLW}{Row metric for Y (M_Y)}

\item{XRW}{Column metric for X (W_X)}

\item{YRW}{Column metric for Y (W_Y)}

\item{k}{Number of components. If \code{k} exceeds
\code{min(ncol(X), ncol(Y))}, a warning is issued and \code{k} is silently
truncated to that maximum.}

\item{center, scale}{Logical; pre-center/scale columns of X, Y before metrics}

\item{svd_backend}{One of "eigencore" (default) or "irlba"; "RSpectra" is
accepted as a deprecated alias of "eigencore". Ignored
whenever both \code{ncol(X) <= 64} and \code{ncol(Y) <= 64}, in which case \code{S} is
materialized densely and solved with \code{base::svd()}.}

\item{svd_opts}{List of options for the backend: \code{tol} (both backends) and
\code{maxitr} (irlba only; the eigencore partial SVD has no iteration cap).
An incomplete eigencore solve raises \code{genpca_solver_nonconvergence};
try a less stringent \code{tol} if the requested accuracy cannot be reached.}

\item{constraints_remedy}{What to do with a metric that is not positive
semi-definite: \code{"error"} (default), \code{"ridge"}, \code{"clip"} or \code{"identity"};
repairs emit a \code{genpca_metric_repaired} warning. See \code{\link[=genpca]{genpca()}}.}
}
\value{
A list with elements:
\describe{
\item{d}{Length-\code{k} numeric vector of singular values of
\eqn{S = Xe' Ye}.}
\item{u}{\verb{I x k} matrix; left singular vectors of \code{S} (orthonormal in
the Euclidean metric).}
\item{v}{\verb{J x k} matrix; right singular vectors of \code{S} (orthonormal in
the Euclidean metric).}
\item{p}{\verb{I x k} matrix of generalized X-weights,
\eqn{p = W_X^{-1/2} u}.}
\item{q}{\verb{J x k} matrix of generalized Y-weights,
\eqn{q = W_Y^{-1/2} v}.}
\item{fi}{\verb{I x k} matrix of X-variable scores, \eqn{F_i = W_X p D}
(columns of \code{p} rescaled by the singular values).}
\item{fj}{\verb{J x k} matrix of Y-variable scores, \eqn{F_j = W_Y q D}.}
\item{lx}{\verb{N x k} matrix of X row latent variables,
\eqn{L_x = M_X^{1/2} X W_X p}.}
\item{ly}{\verb{N x k} matrix of Y row latent variables,
\eqn{L_y = M_Y^{1/2} Y W_Y q}.}
\item{k}{Integer; number of components actually returned (may be
less than the requested \code{k} if it exceeded \code{min(I, J)}).}
\item{dims}{A list \code{list(N, I, J)} with the row count \code{N} and column
counts \code{I = ncol(X)}, \code{J = ncol(Y)}.}
\item{center}{A list \code{list(X, Y)} of the length-\code{I} / length-\code{J}
column means subtracted from \code{X}/\code{Y} (all zero when
\code{center = FALSE}).}
\item{scale}{A list \code{list(X, Y)} of the length-\code{I} / length-\code{J}
column scale factors divided out of \code{X}/\code{Y} (all one when
\code{scale = FALSE}).}
}
}
\description{
Compute the top-k singular triplets of \eqn{S = Xe' Ye} without
materializing the whitened matrices \eqn{Xe = Mx^{1/2} X Wx^{1/2}},
\eqn{Ye = My^{1/2} Y Wy^{1/2}} when doing so would densify sparse data.
When the whitening is sparsity-preserving (identity/diagonal metrics) or
the data are dense, the whitened blocks are precomputed once so each
matrix-vector product in the iterative SVD costs two multiplies.
}
\details{
Naming map: this function names its metrics \code{XLW}/\code{YLW} (left/row
weights) and \code{XRW}/\code{YRW} (right/column weights); these correspond to
\code{Mx}/\code{My} (row metrics) and \code{Ax}/\code{Ay} (column metrics) in \code{genpca()}'s and
\code{genpls()}'s M/A convention.
}
\examples{
set.seed(1)
X <- matrix(rnorm(40 * 6), 40, 6)
Y <- matrix(rnorm(40 * 4), 40, 4)
op <- gplssvd_op(X, Y, k = 2, center = TRUE)
round(op$d, 3)
}
\references{
Beaton, D. (2020). Generalized eigen, singular value, and partial least
squares decompositions: The GSVD package. arXiv:2010.14734.

Abdi, H. (2007). Partial least square regression PLS-Regression. In
N. Salkind (Ed.), \emph{Encyclopedia of Measurement and Statistics}.
Thousand Oaks, CA: Sage.
}
