% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints_utils.R
\name{is_psd}
\alias{is_psd}
\alias{is_pd}
\alias{is_spd}
\title{Test positive semi-definiteness (relative tolerance)}
\usage{
is_psd(A, rtol = .metric_rtol_default())

is_pd(A, rtol = .metric_rtol_default())

is_spd(A, tol = .metric_rtol_default())
}
\arguments{
\item{A}{numeric matrix or Matrix::Matrix}

\item{rtol}{relative tolerance (default \code{sqrt(.Machine$double.eps)})}

\item{tol}{relative tolerance (deprecated name; same as \code{rtol})}
}
\value{
logical
}
\description{
\code{is_psd()} is TRUE when \code{A} is symmetric and every
eigenvalue exceeds \code{-rtol * max(abs(diag(A)))}; \code{is_pd()} is TRUE when
every eigenvalue exceeds \code{+rtol * max(abs(diag(A)))}. Both are shifted
Cholesky probes, so large sparse matrices never need an
eigendecomposition. \code{is_spd()} is a deprecated alias of \code{is_psd()} kept
for internal callers (its \code{tol} is the relative tolerance).
}
\keyword{internal}
