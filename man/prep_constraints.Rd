% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpca.R
\name{prep_constraints}
\alias{prep_constraints}
\title{Prepare and validate constraint matrices}
\usage{
prep_constraints(
  X,
  A,
  M,
  tol = .metric_rtol_default(),
  remedy = c("error", "ridge", "clip", "identity"),
  verbose = FALSE
)
}
\arguments{
\item{X}{data matrix (only its dimensions are used)}

\item{A, M}{column/row constraints}

\item{tol}{relative PSD tolerance (default \code{sqrt(.Machine$double.eps)})}

\item{remedy}{what to do with an indefinite metric}

\item{verbose}{emit a message when a metric is replaced by the identity}
}
\value{
list with elements \code{A} and \code{M}
}
\description{
Coerces \code{A}/\code{M} (NULL, weight vector, diagonal, dense or
sparse matrix) to \code{Matrix} objects and validates them: finite entries,
symmetry within roundoff (see \code{\link[=symmetrize_or_stop]{symmetrize_or_stop()}}), and positive
semi-definiteness within \code{tol} relative to the scale of the matrix. The
requested \code{remedy} is applied to a metric that fails the PSD check (or has
negative eigenvalues within tolerance when explicit clipping is requested),
and every repair emits a warning of class \code{genpca_metric_repaired} carrying
the \code{\link[=repair_metric]{repair_metric()}} report; valid PSD metrics, singular ones included,
pass through under every remedy. Explicit clipping removes even negative
eigenvalues within the validation tolerance. Asymmetric input is an error
under every remedy.
}
\keyword{internal}
