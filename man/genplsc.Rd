% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genplsc.R
\name{genplsc}
\alias{genplsc}
\title{Canonical Generalized PLS (alias)}
\usage{
genplsc(
  X,
  Y,
  Ax = NULL,
  Ay = NULL,
  Mx = NULL,
  My = NULL,
  ncomp = 2,
  preproc_x = multivarious::pass(),
  preproc_y = multivarious::pass(),
  svd_backend = c("eigencore", "irlba", "RSpectra"),
  svd_opts = list(tol = 1e-07, maxitr = 1000),
  constraints_remedy = c("error", "ridge", "clip", "identity"),
  verbose = FALSE
)
}
\arguments{
\item{X}{Numeric or Matrix, n x p.}

\item{Y}{Numeric or Matrix, n x q. Must have same n as \code{X}.}

\item{Ax}{Column metric for X (W_X): vector/diagonal/matrix; \code{NULL} means identity.}

\item{Ay}{Column metric for Y (W_Y): vector/diagonal/matrix; \code{NULL} means identity.}

\item{Mx}{Row metric for X (M_X): vector/diagonal/matrix; \code{NULL} means identity.}

\item{My}{Row metric for Y (M_Y): vector/diagonal/matrix; \code{NULL} means identity.}

\item{ncomp}{Number of components to extract (rank-k). Default 2.}

\item{preproc_x, preproc_y}{Optional \code{multivarious} preprocessors (e.g., \code{center()}).
Defaults to \code{multivarious::pass()} (no-op).}

\item{svd_backend}{Character, one of \code{"eigencore"} (default) or \code{"irlba"} for
the iterative SVD. This choice only matters for larger problems: whenever
both \code{X} and \code{Y} have at most 64 columns after preprocessing, the
operator materializes \code{S} densely and computes a direct \code{svd()},
ignoring \code{svd_backend} entirely (see \code{gplssvd_op()}).}

\item{svd_opts}{List of options: \code{tol} for both backends and \code{maxitr} for
irlba only. An incomplete eigencore solve raises an error of class
\code{genpca_solver_nonconvergence}; no unchecked fit is returned.}

\item{constraints_remedy}{What to do with a metric that is not positive
semi-definite: \code{"error"} (default), \code{"ridge"}, \code{"clip"} or \code{"identity"};
repairs emit a \code{genpca_metric_repaired} warning. See \code{\link[=genpca]{genpca()}}.}

\item{verbose}{Logical; print brief progress messages.}
}
\value{
An object of class \code{c("genpls", "cross_projector", "projector")}
with the same structure as \code{genpls()} returns (X-/Y-weights \code{vx}/\code{vy},
singular values \code{d}, generalized weights \code{p}/\code{q}, scores \code{fi}/\code{fj},
latent variables \code{lx}/\code{ly}, \code{ncomp}, and \code{backend}); see \code{?genpls} for
the definition of each slot.
}
\description{
Convenience alias for \code{genpls()}; computes canonical generalized PLS
(PLS-SVD/GPLSSVD). See \code{?genpls} for full documentation.
}
\examples{
set.seed(1)
X <- matrix(rnorm(60 * 5), 60, 5)
Y <- matrix(rnorm(60 * 4), 60, 4)
fit <- genplsc(X, Y, ncomp = 2,
               preproc_x = multivarious::center(),
               preproc_y = multivarious::center())
fit$d
}
\references{
Beaton, D. (2020). Generalized eigen, singular value, and partial least
squares decompositions: The GSVD package. (Eqs. 10-14). arXiv:2010.14734.
}
\seealso{
\code{\link[=genpls]{genpls()}}
}
