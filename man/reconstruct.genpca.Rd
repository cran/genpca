% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpca.R
\name{reconstruct.genpca}
\alias{reconstruct.genpca}
\title{Reconstruct data from a genpca fit}
\usage{
\method{reconstruct}{genpca}(
  x,
  comp = 1:multivarious::ncomp(x),
  rowind = NULL,
  colind = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{genpca} object.}

\item{comp}{Integer vector of components to use (default: all).}

\item{rowind}{Optional integer vector of rows to reconstruct (default: all).}

\item{colind}{Optional integer vector of columns to reconstruct (default:
all). The inverse preprocessing transform is applied to the selected
columns.}

\item{...}{Ignored.}
}
\value{
A numeric matrix of dimension \verb{length(rowind) x length(colind)}.
}
\description{
Reconstructs (an approximation of) the original data from a \code{genpca} fit as
\code{ou[, comp] \%*\% diag(d[comp]) \%*\% t(ov[, comp])}, followed by the inverse of
the preprocessing transform. With all components and full rank this
recovers the original data.
}
\examples{
X <- matrix(rnorm(60), 15, 4)
fit <- genpca(X, ncomp = 4, preproc = multivarious::center())
max(abs(reconstruct(fit) - X)) # ~ 0 at full rank
}
\seealso{
\code{\link[=genpca]{genpca()}}, \code{\link[=truncate.genpca]{truncate.genpca()}}
}
