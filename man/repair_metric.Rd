% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repair_metric.R
\name{repair_metric}
\alias{repair_metric}
\title{Repair a metric matrix explicitly}
\usage{
repair_metric(
  A,
  method = c("ridge", "clip", "identity"),
  rtol = .metric_rtol_default(),
  name = "A",
  diag_maxn = 2000L
)
}
\arguments{
\item{A}{A square symmetric matrix (base matrix or \code{Matrix}). Asymmetry
beyond roundoff is an error (see the relative asymmetry test in
\code{symmetrize_or_stop()}); it is not something a PSD repair should hide.}

\item{method}{\code{"ridge"} adds a diagonal loading that makes the
matrix positive definite (Gershgorin-based shift, falling back to
\code{Matrix::nearPD()} for small dense matrices); \code{"clip"} projects onto the
PSD cone by zeroing negative eigenvalues (dense eigendecomposition; refuses
large sparse input); \code{"identity"} replaces an indefinite matrix by the
identity (the report still describes the input).}

\item{rtol}{Relative tolerance: eigenvalues above \code{-rtol * scale(A)} count
as non-negative for \code{"ridge"} and \code{"identity"}, which then return the
matrix unchanged. \code{"clip"} always removes negative eigenvalues, regardless
of this tolerance (up to reconstruction roundoff). Default
\code{sqrt(.Machine$double.eps)}.}

\item{name}{Label used in messages.}

\item{diag_maxn}{Largest dimension for which the report computes the full
spectrum (minimum eigenvalue, rank, condition number); above it an
iterative minimum eigenvalue estimate and a Gershgorin bound are reported,
with rank and condition number unavailable.}
}
\value{
The repaired matrix (a \code{Matrix}), with attribute \code{"repair_report"}
of class \code{"metric_repair_report"}: a list with \code{name}, \code{method},
\code{changed}, \code{n}, \code{min_eigenvalue_before}, \code{min_eigenvalue_after},
\code{gershgorin_bound_before}, \code{shift} (diagonal loading added by \code{"ridge"},
\code{NA} for \code{"clip"}), \code{rank}, \code{condition_number} and \code{rtol}.
}
\description{
Returns a positive (semi)definite version of \code{A} together with a
diagnostic report of what was done. This is the explicit counterpart of
the \code{constraints_remedy} argument of \code{\link[=genpca]{genpca()}}: nothing in the package
repairs a metric silently, and this function lets you inspect the repair
before using the result.
}
\examples{
A <- matrix(c(1, 2, 2, 1), 2)           # eigenvalues 3 and -1
B <- repair_metric(A, method = "ridge")
attr(B, "repair_report")
C <- repair_metric(A, method = "clip")
eigen(as.matrix(C))$values
}
\seealso{
\code{\link[=genpca]{genpca()}} (argument \code{constraints_remedy})
}
